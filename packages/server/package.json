{
  "name": "server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot --inspect ./serve.ts",
    "start": "bun run ./serve.ts",
    "tsc": "bun run tsc"
  },
  "peerDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@gotpop-platform/types": "workspace:*"
  }
}
